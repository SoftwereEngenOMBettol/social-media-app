# Navigate to client folder
cd client

# Create or update .env file for production
cat > .env << 'EOF'
# Server URL - production (deployment)
REACT_APP_SERVER_URL=https://postit-app-server.onrender.com

# Server URL - development (local server) - COMMENTED OUT
# REACT_APP_SERVER_URL=http://localhost:3001
EOF

# Verify the file was created
cat .env
